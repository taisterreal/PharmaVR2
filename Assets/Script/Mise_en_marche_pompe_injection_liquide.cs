using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mise_en_marche_pompe_injection_liquide : MonoBehaviour {

    //GameObject check_cable_pression;

    // Use this for initialization
    void Start () {
        //check_produit.GetComponent<Toggle>().isOn = true;

        //check_produit = GameObject.Find("Introduction du produit dans la cuve");

        //check_produit.GetComponent<Toggle>().isOn = true;
    }

    // Update is called once per frame
    void Update () {
		
	}
}
